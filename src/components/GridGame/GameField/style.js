import styled from '@emotion/styled'
import tw from 'tailwind.macro'




const StyledGameField=styled('div')`${tw`flex flex-wrap m-8 `} width:${props=>props.gridWidth}px; `

export default StyledGameField